name: CLI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cargo build -p cli --verbose
    - name: Run tests
      run: cargo test -p cli --verbose
